<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">

    <title>Document</title>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1><%= assignment.title %></h1>
        </div>
    <div class="details">
        <p>Detail: <%= assignment.details %></p>
    </div>

    <div class="reference">
        <p>Reference: <a href="<%= assignment.fileURL %>"><%= fileName %></a></p>
    </div>

     <div class="due-date">
	    	<p>Due Date: <%= assignment.dueDate %></p>
     </div>
     
     <div class="mark">
         <p>Marks: <%= assignment.marks %></p>
     </div>
    
     <% if(!isSubmitted){ %>
     <form action="/project/submit/<%= assignment._id %>" method="POST" enctype="multipart/form-data">
        <div class="button upload">
            <p>Upload</p> <input type="file" name="file" required> 
        </div> 
        <br>
    
    
        <input class="button" type="submit" 
        name="submit" value="Submit">
     </form>
    <% }else{ %>
       Given Marks: <%= completedAssignment.givenMarks > 0 ? completedAssignment.givenMarks : `pending` %> 
    <% } %>
    </div>    
</body>
</html>