<%- include('partials/header')%>
<%- include('partials/nav')%>

<div class="container">
  <% if(assignments.length < 1) {%>
  <div class="m-2 p-1 border-bottom text-center">
    <h1>Your Assignments will show here.</h1>
  </div>
  <% } else {%>
  <div class="m-2 p-1 border-bottom text-center">
    <h1>Assignments</h1>
  </div>
  <div class="row">
    <% assignments.forEach((assignment, i)=>{ %>
    <div class="col-12 col-md-6 col-lg-4 mt-2">
      <div class="card text-center h-100">
        <img src="https://images.unsplash.com/photo-1501618669935-18b6ecb13d6d?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1149&q=80" class="card-img-top" alt="...">
        <div class="card-body">
          <h5 class="card-title"><%= assignment.title %></h5>
          <p class="card-text"><%= assignment.details %></p>
          <a href="/admin/project/<%= assignment._id %>" class="btn btn-primary">View</a>
        </div>
      </div>
    </div>
    <% }); %>
  </div>
  <% } %>
</div>

<%- include('partials/footer')%>